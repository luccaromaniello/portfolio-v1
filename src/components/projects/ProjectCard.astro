---
import ProjectLink from "@components/projects/ProjectLink.astro";
import { Image } from "astro:assets";

interface Props {
  title: string;
  description: string;
  website: {
    href: string;
    external?: boolean;
  };
  repository?: string;
  image: {
    src: ImageMetadata;
    alt: string;
  };
  even?: boolean;
}

const {
  title,
  description,
  website,
  repository,
  image,
  even = false,
} = Astro.props;
---

<div
  class={`flex flex-col ${even ? "md:flex-row-reverse" : "md:flex-row"} md:gap-40 2xl:gap-56`}
>
  <div class="flex flex-col gap-6 justify-center md:w-2/3 2xl:w-1/2">
    <div class="flex flex-col gap-2">
      <h3
        class="text-3xl md:text-6xl sm:text-center md:text-start uppercase font-bold text-white"
      >
        {title}
      </h3>
      <p class="py-2 text-lg md:text-xl font-light text-content-secondary">
        {description}
      </p>
    </div>
    <div class="flex flex-col md:flex-row gap-6">
      <ProjectLink label="View project" href={website.href} />
      {repository && <ProjectLink label="View repository" href={repository} />}
    </div>
  </div>
  <div class="md:w-1/3 2xl:w-1/2 h-full">
    {image && <Image src={image.src} alt={image.alt} />}
  </div>
</div>
